{
  "name": "zhengang-tower",
  "private": true,
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "dev:client": "pnpm --filter zhengang-tower-client run dev:weapp",
    "build:client": "pnpm --filter zhengang-tower-client run build:weapp"
  },
  "engines": {
    "node": ">=14",
    "pnpm": ">=7"
  },
  "dependencies": {
    "@tarojs/plugin-platform-weapp": "4.0.9"
  },
  "devDependencies": {
    "@babel/preset-typescript": "^7.27.0",
    "babel-loader": "^10.0.0"
  }
}
